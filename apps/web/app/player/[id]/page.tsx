"use client";
import { useRef, useState } from "react";
import { Captions, Maximize, Pause, Play, Settings, Volume2 } from "lucide-react";
export default function PlayerPage(){const ref=useRef<HTMLVideoElement>(null);const[playing,setPlaying]=useState(false);function toggle(){const v=ref.current;if(!v)return;if(v.paused)void v.play();else v.pause();setPlaying(!v.paused)}return <div className="player-page"><div className="video-stage"><video ref={ref} poster="https://s4.anilist.co/file/anilistcdn/media/anime/banner/21827-3EwjBS6ebj1C.jpg" onClick={toggle}/><button className="center-play" onClick={toggle}>{playing?<Pause fill="currentColor"/>:<Play fill="currentColor"/>}</button><div className="player-controls"><button onClick={toggle}>{playing?<Pause/>:<Play/>}</button><Volume2/><span>00:00</span><div className="video-track"><i/></div><span>24:00</span><Captions/><Settings/><Maximize/></div></div><div className="now-playing"><div><span className="eyebrow">NOW PLAYING · PERSONAL MEDIA</span><h1>Sample episode</h1><p>Connect Jellyfin or add an authorized HLS/MP4 URL to begin playback.</p></div><select aria-label="Episode"><option>Episode 1</option></select></div></div>}
