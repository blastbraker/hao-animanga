"use client";
import { useState } from "react";
import { BookOpen, Check, Cloud, Film, HardDrive, KeyRound, Link2, Plus, Server, ShieldAlert, Upload } from "lucide-react";
import { api } from "../../lib/api";
const DISCLAIMER="Third-party repositories and extensions are not created, reviewed, hosted, endorsed, supported, or controlled by HAO. Their developers and content providers are unaffiliated with HAO. Availability, safety, and legality are not guaranteed. You are responsible for using only content you are authorized to access and for complying with applicable laws and provider terms.";
export default function SettingsPage(){const[repo,setRepo]=useState("");const[ack,setAck]=useState(false);const[code,setCode]=useState<string|null>(null);async function pair(){const x=await api<{code:string}>("/bridges/pairing-code",{method:"POST"});setCode(x.code)}return <div className="page inner-page settings-page"><div className="page-intro"><span className="eyebrow">MAKE HAO YOURS</span><h1>Sources & devices</h1><p>Your library stays independent. Sources can change without taking your history with them.</p></div><section className="settings-section"><h2>Content connections</h2><div className="settings-grid"><Source icon={<Film/>} title="Jellyfin" copy="Stream from your personal media server." action="Connect"/><Source icon={<Link2/>} title="Direct media" copy="Add an authorized HTTPS HLS or MP4 URL." action="Add URL"/><Source icon={<BookOpen/>} title="EPUB library" copy="Upload books to your private encrypted storage." action="Upload"/><Source icon={<Cloud/>} title="AniList" copy="Catalog metadata and discovery." action="Active" active/></div></section><section className="settings-section bridge-panel"><div><span className="eyebrow">USER-OWNED RUNTIME</span><h2>HAO Bridge Desktop</h2><p>Run manga and anime extensions on a device you control. Third-party APKs never execute in HAO’s cloud.</p>{code?<div className="pair-code"><KeyRound/> Pairing code <b>{code}</b></div>:<button className="button primary" onClick={()=>void pair()}><Server/> Generate pairing code</button>}</div><div className="bridge-visual"><HardDrive/><span>Bridge offline</span><small>Windows · macOS · Linux</small></div></section><section className="settings-section"><span className="eyebrow">EXTENSION REPOSITORIES</span><h2>Add repository</h2><div className="repo-form"><input type="url" value={repo} onChange={e=>setRepo(e.target.value)} placeholder="https://…/index.min.json"/><select aria-label="Repository type"><option>Manga · Mihon</option><option>Anime · Aniyomi</option></select></div><div className="warning-box"><ShieldAlert/><div><b>Before you continue</b><p>{DISCLAIMER}</p><label><input type="checkbox" checked={ack} onChange={e=>setAck(e.target.checked)}/><span>I understand and accept responsibility for this repository.</span></label></div></div><button className="button primary" disabled={!ack||!repo.startsWith("https://")}><Plus/> Validate and add</button></section></div>}
function Source({icon,title,copy,action,active}:{icon:React.ReactNode;title:string;copy:string;action:string;active?:boolean}){return <article className="source-card"><span className="source-icon">{icon}</span><div><h3>{title}</h3><p>{copy}</p></div><button className={active?"connected":""}>{active?<Check/>:<Plus/>}{action}</button></article>}
