app = "hao-worker"
primary_region = "iad"

[build]
  dockerfile = "apps/worker/Dockerfile"

[env]
  NODE_ENV = "production"

[processes]
  app = "node apps/worker/dist/index.js"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
